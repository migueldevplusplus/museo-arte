# Museum System Documentation

## A. Problem Statement
The system manages the exhibition and sale of artworks for the Museum of Contemporary Art. It handles:
- **Artworks**: Categorized by genre (Painting, Sculpture, etc.) with specific attributes.
- **Artists**: Information including biography and genres.
- **Users**:
  - **Buyers**: Register, pay membership ($10), receive security code, reserve artworks.
  - **Employees (Admins)**: Manage CRUD, finalize sales, generate invoices, view reports.
- **Process**: Buyer reserves -> Employee contacts -> Employee creates Sale (Invoice) -> Status 'SOLD'.

**Additions/Assumptions**:
- Added `BuyerProfile` to separate buyer-specific data (credit card, shipping).
- Added `Membership` model to track payment history.
- Used Django Admin for the "employee" interface to CRUD data and Reports.

## B. Use Cases
1. **Visitor**:
   - View Home Page (Featured works)
   - Search Catalog (Filter by Artist, Genre, Price)
   - Register (Become Buyer)
2. **Buyer**:
   - Log In
   - Reserve Artwork (Enter Security Code)
3. **Admin (Employee)**:
   - CRUD Artists, Genres, Artworks
   - Finalize Sale (Create Invoice)
   - View Sales Report
   - View Membership Report

## C. Tools Used
- **Language**: Python 3.x
- **Framework**: Django 6.0.2
- **DBMS**: MySQL
- **Frontend**: HTML5, Bootstrap 5

## D. ER Model & Class Diagram Description
*Since I cannot generate images directly, here is the text description:*

### Entities
- **User** (Django Auth): `username`, `password`, `email`, `is_buyer`, `is_employee`.
- **BuyerProfile**: `user_id` (FK), `credit_card`, `security_code`, `address`.
- **Genre**: `name`.
- **Artist**: `name`, `bio`, `nationality`, `birth_date`, `photo`.
- **Artwork**: `title`, `artist_id` (FK), `genre_id` (FK), `price`, `date`, `photo`, `status` (Available/Reserved/Sold), `attributes` (JSON).
- **Sale**: `artwork_id` (FK), `buyer_id` (FK), `date`, `subtotal`, `iva`, `commission`, `total`.
- **Membership**: `buyer_id` (FK), `date`, `amount`.

### Relationships
- Artist M:N Genre
- Artist 1:N Artwork
- Genre 1:N Artwork
- User 1:1 BuyerProfile
- BuyerProfile 1:N Membership
- Artwork 1:1 Sale
- Buyer 1:N Sale

## F. Data Dictionary

### Table: users_user
| Field | Type | Description |
|-------|------|-------------|
| id | Integer | PK |
| username | Varchar | Login ID |
| email | Varchar | Email address |
| is_buyer | Boolean | Flag for buyers |
| is_employee| Boolean | Flag for staff |

### Table: museum_artwork
| Field | Type | Description |
|-------|------|-------------|
| id | Integer | PK |
| title | Varchar | Name of artwork |
| artist_id | Integer | FK to Artist |
| genre_id | Integer | FK to Genre |
| price | Decimal | Sale price |
| status | Varchar | Available, Reserved, Sold |
| attributes | JSON | Flexible attributes (dimensions, etc.) |

*( ... and so on for other tables ... )*

## G. Screenshots
*(Please take screenshots of the running application)*
- **Home**: Featured artworks.
- **Catalog**: Sidebar filters and grid of artworks.
- **Register**: Form with credit card field.
- **Artwork Detail**: Image, info, and "Reserve" button.
- **Admin**: Sales list and creation form.

## H. SQL Script
The SQL structure is generated by Django Migrations. A dump can be obtained via `mysqldump`.
